[project]
name = "localint"
version = "0.3.0"
description = "LocaLint: game localization QA before release."
requires-python = ">=3.11"
dependencies = [
  "streamlit>=1.36",
  "pandas>=2.2",
  "pydantic>=2.7",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
